# Normalize line endings to LF in the repo so shell scripts stay executable
# inside Linux containers regardless of the contributor's OS.
* text=auto eol=lf

*.sh text eol=lf

# Vendored minified assets: keep as-is, don't diff.
static/vendor/** binary
